#This file converts a single frame from a multistack tiff into a numpy array
#the tifffile library and numpy library must be downloaded. 

#import the tifffile and numpy libraries 
import tifffile as tf
import numpy as np

#EXAMPLE OF A CURRENT PATH C:\Users\Name\Documents\image.tif
#EXAMPLE OF A CURRENT PATH C:\Users\Name\Documents\array.npy

current_path = r"ENTER PATH TO FILE HERE"

new_path = r"ENTER PATH TO NEW FILE HERE"

#the key refers to the frame or slice you want to use
image = tf.imread(current_path, key = 0)

print(image.shape)


np.save(new_path, image)
print("numpy array saved!")
